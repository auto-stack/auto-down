---
'@autodown/engine': minor
---

plan 021: editor chrome layer re-Auto-ized. The 12 plan-013 widget products are live generated sources again — `menus/{SlashMenu,BubbleMenu,TableMenu,CodeBlockMenu}.vue`, `components/CodeLanguageIcon.vue` and the seven `node-views/*.vue` regenerate from `auto/editor/*.at` (`pnpm gen:editor`; byte-identical to the last Tiptap-era deployments modulo ext import specifiers). The seven hand-written ext bridges deploy to `src/editor/ext/`, fully retargeted off Tiptap onto engine interfaces (SlashMenu's `computeMenuPosition` now routes through the bridge; bubble/table/codeblock menus carry dormant engine hosts pending the inline-mark/menu-host extension, see DEBTS 020/021). `createEditorAdapter` now carries an OPTIONAL `__engine` back-reference (additive to the 1.0.0 frozen surface) so engine-native slash-manifest readers (Block link anchoring) reach the session. New build guard `assert-editor-gen.mjs`: every deployed product must have its `.at` source, the deployed set is exact, and ext bridges stay byte-synced — no frozen products without sources can land again. Boundary ruling: `EngineEditor`/`BlockHost` stay the handwritten platform assembly shell (IME/composition-dense); the dormant `auto_down_editor.at` reference implementation stays undeployed (ARCHITECTURE.md §6).
