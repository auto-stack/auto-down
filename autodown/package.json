{
  "name": "autodown",
  "private": true,
  "version": "0.0.0",
  "description": "AutoDown — Spec-driven document format with Vue renderer and Tiptap editor",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "typescript": "^5.4.0",
    "vite": "^5.0.0",
    "vue": "3.5.35",
    "vue-tsc": "^2.0.0",
    "vitest": "^1.6.0"
  }
}
