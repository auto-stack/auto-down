{
  "name": "@autodown/core",
  "version": "0.4.0",
  "description": "AutoDown core utilities — re-export shim for @autodown/engine/parser (plan 017)",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node -e \"require('fs').rmSync('tsconfig.tsbuildinfo',{force:true})\" && tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@autodown/engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  },
  "deprecated": "@autodown/engine supersedes this package (plan 017; retirement ruling plan 020 Phase 4). Consume the engine exits directly: this package maps to @autodown/engine/{parser,render,editor}."
}
