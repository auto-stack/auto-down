// Auto-generated by a2r transpiler

use crate::ial::{preprocessMarkdown, startsWithStr, startsWithAt, endsWithStr, trimStartStr, trimEndStr, hasChar, findStr, findStrFrom, rfindChar, scanIntPrefix, TableAttr};
use crate::block_model::{BlockNode, BlockType, InlineSpan, Mark, Attr, Value, SourceRange, rng, span, attrSet, addMark, spansText, spanWith, blockFull, attrOf, withIdAndAnchor};
#[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
pub struct WNode {
    pub r#type: String,
    pub content: Option<String>,
    pub level: Option<i64>,
    pub language: Option<String>,
    pub code: Option<String>,
    pub loading: Option<bool>,
    pub children: Option<Vec<WNode>>,
    pub ordered: Option<bool>,
    pub start: Option<i64>,
    pub items: Option<Vec<WNode>>,
    pub cells: Option<Vec<WNode>>,
    pub header: Option<Vec<WNode>>,
    pub rows: Option<Vec<WNode>>,
    pub isHeader: Option<bool>,
    pub align: Option<String>,
    pub href: Option<String>,
    pub title: Option<String>,
    pub text: Option<String>,
    pub src: Option<String>,
    pub alt: Option<String>,
    pub checked: Option<bool>,
}

pub fn noNodes() -> Vec<WNode> {
    let mut l: Vec<WNode> = vec![];
    return l;
}

fn codeNode(language: &str, code: &str, loading: bool) -> WNode {
    return WNode { r#type: "code_block".to_string(), content: None, level: None, language: Some(language.to_string()), code: Some(code.to_string()), loading: Some(loading), children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn headingNode(level: i64, children: Vec<WNode>) -> WNode {
    return WNode { r#type: "heading".to_string(), content: None, level: Some(level), language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn thematicNode() -> WNode {
    return WNode { r#type: "thematic_break".to_string(), content: None, level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn quoteNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "blockquote".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn paraNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "paragraph".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn tableNode(header: Vec<WNode>, rows: Vec<WNode>, loading: bool) -> WNode {
    return WNode { r#type: "table".to_string(), content: None, level: None, language: None, code: None, loading: Some(loading), children: None, ordered: None, start: None, items: None, cells: None, header: Some(header), rows: Some(rows), isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn rowNode(cells: Vec<WNode>) -> WNode {
    return WNode { r#type: "table_row".to_string(), content: None, level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: Some(cells), header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn cellNode(isHeaderCell: bool, children: Vec<WNode>, align: &str) -> WNode {
    return WNode { r#type: "table_cell".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: Some(isHeaderCell), align: Some(align.to_string()), href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn listNode(ordered: bool, startN: Option<i64>, items: Vec<WNode>) -> WNode {
    return WNode { r#type: "list".to_string(), content: None, level: None, language: None, code: None, loading: None, children: None, ordered: Some(ordered), start: startN, items: Some(items), cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn itemNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "list_item".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn strongNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "strong".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn emNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "emphasis".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn underlineNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "underline".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn strikeNode(children: Vec<WNode>) -> WNode {
    return WNode { r#type: "strikethrough".to_string(), content: None, level: None, language: None, code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn codeSpanNode(code: &str) -> WNode {
    return WNode { r#type: "inline_code".to_string(), content: None, level: None, language: None, code: Some(code.to_string()), loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn hardbreakNode() -> WNode {
    return WNode { r#type: "hardbreak".to_string(), content: None, level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn imageNode(src: &str, alt: &str) -> WNode {
    return WNode { r#type: "image".to_string(), content: None, level: None, language: None, code: None, loading: Some(false), children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: Some(src.to_string()), alt: Some(alt.to_string()), checked: None };
}

fn linkNode(href: &str, title: Option<String>, textContent: &str, children: Vec<WNode>, loading: bool) -> WNode {
    return WNode { r#type: "link".to_string(), content: None, level: None, language: None, code: None, loading: Some(loading), children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: Some(href.to_string()), title: title, text: Some(textContent.to_string()), src: None, alt: None, checked: None };
}

fn calloutNode(ctype: &str, title: &str, children: Vec<WNode>) -> WNode {
    return WNode { r#type: "callout".to_string(), content: None, level: None, language: Some(ctype.to_string()), code: None, loading: None, children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: Some(title.to_string()), text: None, src: None, alt: None, checked: None };
}

fn detailsNode(summary: &str, openFlag: bool, children: Vec<WNode>) -> WNode {
    return WNode { r#type: "details".to_string(), content: None, level: None, language: None, code: None, loading: Some(openFlag), children: Some(children), ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: Some(summary.to_string()), src: None, alt: None, checked: None };
}

fn queryNode(query: &str) -> WNode {
    return WNode { r#type: "query".to_string(), content: Some(query.to_string()), level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn embedNode(src: &str) -> WNode {
    return WNode { r#type: "embed".to_string(), content: None, level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: Some(src.to_string()), alt: None, checked: None };
}

fn rawTextNode(content: &str) -> WNode {
    return WNode { r#type: "text".to_string(), content: Some(content.to_string()), level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

#[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
pub struct DelimScan {
    pub next: i64,
    pub inner: String,
}

#[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
pub struct LinkScan {
    pub next: i64,
    pub text: String,
    pub href: String,
    pub loading: bool,
    pub title: Option<String>,
    pub tail: String,
}

fn normalizeNewlines(src: &str) -> String {



    let a = src.split("\r\n").map(|s| s.to_string()).collect::<Vec<String>>().join("\n");
    return a.split("\r").map(|s| s.to_string()).collect::<Vec<String>>().join("\n");
}

fn stripListMarkerTail(s: &str) -> String {
    let nl = rfindChar(s, 10);
    if nl == -1 {
        return s.to_string();
    }
    let mut p: i64 = nl + 1;
    let mut sp: i64 = 0;
    while p < (s.chars().count() as i64) {
        if s.chars().nth((p) as usize).unwrap_or('\0') as i64 == 32 {
            sp += 1;
            p += 1;
        } else {
            break;
        }

    }
    if sp > 3 {
        return s.to_string();
    }
    if p >= (s.chars().count() as i64) {
        return s.to_string();
    }
    let mut c = s.chars().nth((p) as usize).unwrap_or('\0') as i64;
    let mut after: i64 = -1;
    if c == 45 {
        after = p + 1;
    } else {
        if c == 42 {
            after = p + 1;
        } else {
            if c == 43 {
                after = p + 1;
            }        }
    }

    if after == -1 {
        let mut q: i64 = p;
        let mut digits: i64 = 0;
        while q < (s.chars().count() as i64) {
            let d = s.chars().nth((q) as usize).unwrap_or('\0') as i64;
            if d >= 48 {
                if d <= 57 {
                    digits += 1;
                    q += 1;
                } else {
                    break;
                }
            } else {
                break;
            }

        }
        if digits == 0 {
            return s.to_string();
        }        if digits > 9 {
            return s.to_string();
        }        if q >= (s.chars().count() as i64) {
            return s.to_string();
        }        let dm = s.chars().nth((q) as usize).unwrap_or('\0') as i64;
        if dm == 46 {
            after = q + 1;
        } else {
            if dm == 41 {
                after = q + 1;
            } else {
                return s.to_string();
            }
        }
    }
    let mut t: i64 = after;
    while t < (s.chars().count() as i64) {
        if s.chars().nth((t) as usize).unwrap_or('\0') as i64 == 32 {
            t += 1;
        } else {
            return s.to_string();
        }

    }
    return s.chars().take((nl) as usize).skip((0) as usize).collect::<String>().to_string();
}

fn stripQuoteMarkerTail(s: &str) -> String {
    let nl = rfindChar(s, 10);
    if nl == -1 {
        return s.to_string();
    }
    let mut p: i64 = nl + 1;
    let mut sp: i64 = 0;
    while p < (s.chars().count() as i64) {
        if s.chars().nth((p) as usize).unwrap_or('\0') as i64 == 32 {
            sp += 1;
            p += 1;
        } else {
            break;
        }

    }
    if sp > 3 {
        return s.to_string();
    }
    if p >= (s.chars().count() as i64) {
        return s.to_string();
    }
    if s.chars().nth((p) as usize).unwrap_or('\0') as i64 != 62 {
        return s.to_string();
    }
    let mut t: i64 = p + 1;
    while t < (s.chars().count() as i64) {
        if s.chars().nth((t) as usize).unwrap_or('\0') as i64 == 32 {
            t += 1;
        } else {
            return s.to_string();
        }

    }
    return s.chars().take((nl) as usize).skip((0) as usize).collect::<String>().to_string();
}

fn stripDanglingTail(src: &str, isFinal: bool) -> String {
    if isFinal {
        return src.to_string();
    }
    let mut s: String = src.to_string();
    let mut changed: bool = true;
    while changed {
        changed = false;
        let stripped = stripListMarkerTail(s.as_str());
        if stripped != s {
            s = stripped.to_string();
            changed = true;
        }
        let strippedQuote = stripQuoteMarkerTail(s.as_str());
        if strippedQuote != s {
            s = strippedQuote.to_string();
            changed = true;
        }
    }
    return s;
}

fn isBlank(line: &str) -> bool {
    return line.trim().to_string() == "";
}

fn indentOf(line: &str) -> i64 {
    let mut n: i64 = 0;
    while n < (line.chars().count() as i64) {
        if line.chars().nth((n) as usize).unwrap_or('\0') as i64 == 32 {
            n += 1;
        } else {
            break;
        }

    }
    return n;
}

fn indentWidth(s: &str) -> i64 {
    let mut n: i64 = 0;
    let mut i: i64 = 0;
    while i < (s.chars().count() as i64) {
        let c = s.chars().nth((i) as usize).unwrap_or('\0') as i64;
        if c == 32 {
            n += 1;
        } else {
            if c == 9 {
                n += 4;
            } else {
                break;
            }
        }

        i += 1;
    }
    return n;
}

fn parseDocument(src: &str, isFinal: bool) -> Vec<WNode> {
    let normalized = normalizeNewlines(src);
    let safe = stripDanglingTail(normalized.as_str(), isFinal);
    let lines = safe.split("\n").map(|s| s.to_string()).collect::<Vec<String>>();
    return parseBlocks(lines.clone(), isFinal);
}

fn fenceMarker(line: &str) -> String {
    if indentWidth(line) >= 4 {
        return "".to_string();
    }
    let t = line.trim().to_string();
    if startsWithStr(t.as_str(), "```") {
        return "`".to_string();
    }
    if startsWithStr(t.as_str(), "~~~") {
        return "~".to_string();
    }
    return "".to_string();
}

fn fenceMarkerRun(line: &str) -> String {
    let t = line.trim().to_string();
    let mut run: String = "".to_string();
    let mut i: i64 = 0;
    if (t.chars().count() as i64) == 0 {
        return "".to_string();
    }
    let first = t.chars().nth((0) as usize).unwrap_or('\0') as i64;
    if first != 96 {
        if first != 126 {
            return "".to_string();
        }    }
    while i < (t.chars().count() as i64) {
        if t.chars().nth((i) as usize).unwrap_or('\0') as i64 == first {
            run += &t.chars().take((i + 1) as usize).skip((i) as usize).collect::<String>();
            i += 1;
        } else {
            break;
        }

    }
    return run;
}

fn isCloseFence(line: &str, marker: &str, run: &str) -> bool {
    if indentWidth(line) >= 4 {
        return false;
    }
    let t = line.trim().to_string();
    if (t.chars().count() as i64) != (run.chars().count() as i64) {
        return false;
    }
    let mut i: i64 = 0;
    let mut mkCode: i64 = 96;
    if marker == "~" {
        mkCode = 126;
    }
    while i < (t.chars().count() as i64) {
        if t.chars().nth((i) as usize).unwrap_or('\0') as i64 != mkCode {
            return false;
        }
        i += 1;
    }
    return true;
}

fn headingLevel(line: &str) -> i64 {
    if indentWidth(line) >= 4 {
        return 0;
    }
    let t = line.trim().to_string();
    let mut level: i64 = 0;
    while level < (t.chars().count() as i64) {
        if t.chars().nth((level) as usize).unwrap_or('\0') as i64 == 35 {
            level += 1;
        } else {
            break;
        }

    }
    if level == 0 {
        return 0;
    }
    if level > 6 {
        return 0;
    }
    if level == (t.chars().count() as i64) {
        return level;
    }
    if t.chars().nth((level) as usize).unwrap_or('\0') as i64 != 32 {
        return 0;
    }
    return level;
}

fn stripTrailingHashes(t: &str) -> String {
    let trimmed = t.trim().to_string();
    if (trimmed.chars().count() as i64) == 0 {
        return trimmed;
    }
    if trimmed.chars().nth(((trimmed.chars().count() as i64) - 1) as usize).unwrap_or('\0') as i64 != 35 {
        return trimmed;
    }
    let mut hs: i64 = (trimmed.chars().count() as i64) - 1;
    while hs > 0 {
        if trimmed.chars().nth((hs - 1) as usize).unwrap_or('\0') as i64 == 35 {
            hs -= 1;
        } else {
            break;
        }

    }
    let mut ss: i64 = hs;
    while ss > 0 {
        if trimmed.chars().nth((ss - 1) as usize).unwrap_or('\0') as i64 == 32 {
            ss -= 1;
        } else {
            break;
        }

    }
    if ss == hs {
        return trimmed;
    }
    let body = trimmed.chars().take((ss) as usize).skip((0) as usize).collect::<String>();
    return trimEndStr(body.as_str());
}

fn olMarkerNum(line: &str) -> i64 {
    if indentWidth(line) >= 4 {
        return -1;
    }
    let mut p: i64 = 0;
    let mut sp: i64 = 0;
    while p < (line.chars().count() as i64) {
        if line.chars().nth((p) as usize).unwrap_or('\0') as i64 == 32 {
            sp += 1;
            p += 1;
        } else {
            break;
        }

    }
    if sp > 3 {
        return -1;
    }
    let mut q: i64 = p;
    let mut digits: i64 = 0;
    while q < (line.chars().count() as i64) {
        let d = line.chars().nth((q) as usize).unwrap_or('\0') as i64;
        if d >= 48 {
            if d <= 57 {
                digits += 1;
                q += 1;
            } else {
                break;
            }
        } else {
            break;
        }

    }
    if digits == 0 {
        return -1;
    }
    if digits > 9 {
        return -1;
    }
    if q >= (line.chars().count() as i64) {
        return -1;
    }
    let c = line.chars().nth((q) as usize).unwrap_or('\0') as i64;
    if c != 46 {
        if c != 41 {
            return -1;
        }    }
    let mut after: i64 = q + 1;
    let mut isMarker: bool = false;
    if after == (line.chars().count() as i64) {
        isMarker = true;
    } else {
        if line.chars().nth((after) as usize).unwrap_or('\0') as i64 == 32 {
            isMarker = true;
        }    }

    if !(isMarker) {
        return -1;
    }
    let num = scanIntPrefix(line.chars().take((q) as usize).skip((p) as usize).collect::<String>().as_str());
    return num.unwrap_or(-1);
}

fn bulletMarker(line: &str) -> String {
    if indentWidth(line) >= 4 {
        return "".to_string();
    }
    let mut p: i64 = 0;
    let mut sp: i64 = 0;
    while p < (line.chars().count() as i64) {
        if line.chars().nth((p) as usize).unwrap_or('\0') as i64 == 32 {
            sp += 1;
            p += 1;
        } else {
            break;
        }

    }
    if sp > 3 {
        return "".to_string();
    }
    if p >= (line.chars().count() as i64) {
        return "".to_string();
    }
    let c = line.chars().nth((p) as usize).unwrap_or('\0') as i64;
    let mut isBullet: bool = false;
    if c == 45 {
        isBullet = true;
    } else {
        if c == 42 {
            isBullet = true;
        } else {
            if c == 43 {
                isBullet = true;
            }        }
    }

    if !(isBullet) {
        return "".to_string();
    }
    if p + 1 >= (line.chars().count() as i64) {
        return "".to_string();
    }
    if line.chars().nth((p + 1) as usize).unwrap_or('\0') as i64 != 32 {
        return "".to_string();
    }
    return line.chars().take((p + 1) as usize).skip((p) as usize).collect::<String>().to_string();
}

fn bulletMarkerBare(line: &str) -> bool {
    if indentWidth(line) >= 4 {
        return false;
    }
    let mut p: i64 = 0;
    let mut sp: i64 = 0;
    while p < (line.chars().count() as i64) {
        if line.chars().nth((p) as usize).unwrap_or('\0') as i64 == 32 {
            sp += 1;
            p += 1;
        } else {
            break;
        }

    }
    if sp > 3 {
        return false;
    }
    if p + 1 != (line.chars().count() as i64) {
        return false;
    }
    let c = line.chars().nth((p) as usize).unwrap_or('\0') as i64;
    if c == 45 {
        return true;
    }
    if c == 42 {
        return true;
    }
    if c == 43 {
        return true;
    }
    return false;
}

fn isThematicBreak(line: &str) -> bool {
    if indentWidth(line) >= 4 {
        return false;
    }
    let t = line.trim().to_string();
    if (t.chars().count() as i64) < 3 {
        return false;
    }
    let first = t.chars().nth((0) as usize).unwrap_or('\0') as i64;
    if first != 45 {
        if first != 42 {
            if first != 95 {
                return false;
            }        }    }
    let mut count: i64 = 0;
    let mut i: i64 = 0;
    while i < (t.chars().count() as i64) {
        let c = t.chars().nth((i) as usize).unwrap_or('\0') as i64;
        if c == first {
            count += 1;
        } else {
            if c != 32 {
                return false;
            }        }

        i += 1;
    }
    return count >= 3;
}

fn isSetextUnderline(line: &str) -> i64 {

    if indentWidth(line) >= 4 {
        return 0;
    }
    let t = line.trim().to_string();
    if (t.chars().count() as i64) == 0 {
        return 0;
    }
    let first = t.chars().nth((0) as usize).unwrap_or('\0') as i64;
    if first != 61 {
        if first != 45 {
            return 0;
        }    }
    let mut i: i64 = 0;
    while i < (t.chars().count() as i64) {
        if t.chars().nth((i) as usize).unwrap_or('\0') as i64 != first {
            return 0;
        }
        i += 1;
    }
    if first == 61 {
        return 1;
    }
    return 2;
}

fn startsBlockquote(line: &str) -> bool {
    if indentWidth(line) >= 4 {
        return false;
    }
    return startsWithStr(trimStartStr(line).as_str(), ">");
}

fn quoteBody(line: &str) -> String {
    let t = trimStartStr(line);
    let rest = t.chars().skip((1) as usize).collect::<String>();
    if startsWithStr(rest.as_str(), " ") {
        return rest.chars().skip((1) as usize).collect::<String>().to_string();
    }
    return rest;
}

fn isTableRow(line: &str) -> bool {
    if isBlank(line) {
        return false;
    }
    return hasChar(line, 124);
}

fn isAlignCell(t: &str) -> bool {
    if (t.chars().count() as i64) == 0 {
        return false;
    }
    let mut p: i64 = 0;
    if t.chars().nth((0) as usize).unwrap_or('\0') as i64 == 58 {
        p = 1;
    }
    if p >= (t.chars().count() as i64) {
        return false;
    }
    let mut q: i64 = p;
    while q < (t.chars().count() as i64) {
        if t.chars().nth((q) as usize).unwrap_or('\0') as i64 == 45 {
            q += 1;
        } else {
            break;
        }

    }
    if q == p {
        return false;
    }
    if q == (t.chars().count() as i64) {
        return true;
    }
    if q + 1 == (t.chars().count() as i64) {
        if t.chars().nth((q) as usize).unwrap_or('\0') as i64 == 58 {
            return true;
        }    }
    return false;
}

fn isTableDelimiter(line: &str) -> bool {
    if isBlank(line) {
        return false;
    }
    if !(hasChar(line, 45)) {
        return false;
    }


    let cells = splitRowCells(line);
    if (cells.len() as i64) == 0 {
        return false;
    }
    for c in &cells {
        let t = c.trim().to_string();
        if !(isAlignCell(t.as_str())) {
            return false;
        }
    }
    return true;
}

fn splitRowCells(line: &str) -> Vec<String> {
    let mut t = line.trim().to_string();
    if startsWithStr(t.as_str(), "|") {
        t = t.chars().skip((1) as usize).collect::<String>();
    }
    if endsWithTokSeq(t.as_str(), "|") {
        t = t.chars().take(((t.chars().count() as i64) - 1) as usize).skip((0) as usize).collect::<String>();
    }
    let raw = t.split("|").map(|s| s.to_string()).collect::<Vec<String>>();
    let mut out: Vec<String> = vec![];
    let mut i: i64 = 0;
    while i < (raw.len() as i64) {
        out.push(raw[(i) as usize].clone());
        i += 1;
    }
    return out;
}

fn delimiterAlign(cell: &str) -> String {
    let t = cell.trim().to_string();
    let left = startsWithStr(t.as_str(), ":");
    let right = endsWithTokSeq(t.as_str(), ":");
    if left {
        if right {
            return "center".to_string();
        }        return "left".to_string();
    }
    if right {
        return "right".to_string();
    }
    return "left".to_string();
}

fn isFenceTailRun(t: &str) -> bool {
    if (t.chars().count() as i64) == 0 {
        return false;
    }
    let mut i: i64 = 0;
    while i < (t.chars().count() as i64) {
        let c = t.chars().nth((i) as usize).unwrap_or('\0') as i64;
        if c == 96 {
            

        } else {
            if c == 126 {
                

            } else {
                return false;
            }
        }

        i += 1;
    }
    return true;
}

fn stripTrailingSpaceLine(s: &str) -> String {
    let mut end: i64 = (s.chars().count() as i64);
    while end > 0 {
        if s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64 == 32 {
            end -= 1;
        } else {
            break;
        }

    }
    if end == (s.chars().count() as i64) {
        return s.to_string();
    }
    if end == 0 {
        return s.to_string();
    }
    if s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64 == 10 {
        return s.chars().take((end) as usize).skip((0) as usize).collect::<String>().to_string();
    }
    return s.to_string();
}

fn stripParaIndent(s: &str) -> String {
    let mut n: i64 = 0;
    while n < 4 {
        if n < (s.chars().count() as i64) {
            if s.chars().nth((n) as usize).unwrap_or('\0') as i64 == 32 {
                n += 1;
            } else {
                break;
            }
        } else {
            break;
        }

    }
    if n == 0 {
        return s.to_string();
    }
    return s.chars().skip((n) as usize).collect::<String>().to_string();
}

#[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
pub struct CompScan {
    pub name: String,
    pub argstr: String,
    pub afterParen: i64,
}

fn isIdentChar(c: i64) -> bool {
    if c >= 97 {
        if c <= 122 {
            return true;
        }    }
    if c >= 65 {
        if c <= 90 {
            return true;
        }    }
    if c >= 48 {
        if c <= 57 {
            return true;
        }    }
    if c == 95 {
        return true;
    }
    return false;
}

fn compOpenScan(line: &str) -> Option<CompScan> {
    let mut p: i64 = 0;
    let mut sp: i64 = 0;
    while p < (line.chars().count() as i64) {
        if line.chars().nth((p) as usize).unwrap_or('\0') as i64 == 32 {
            sp += 1;
            p += 1;
        } else {
            break;
        }

    }
    if sp > 3 {
        return None;
    }
    if p >= (line.chars().count() as i64) {
        return None;
    }
    if line.chars().nth((p) as usize).unwrap_or('\0') as i64 != 36 {
        return None;
    }
    let mut q: i64 = p + 1;
    let nameStart: i64 = q;
    while q < (line.chars().count() as i64) {
        if isIdentChar(line.chars().nth((q) as usize).unwrap_or('\0') as i64) {
            q += 1;
        } else {
            break;
        }

    }
    if q == nameStart {
        return None;
    }
    if q >= (line.chars().count() as i64) {
        return None;
    }
    if line.chars().nth((q) as usize).unwrap_or('\0') as i64 != 40 {
        return None;
    }
    let mut r: i64 = q + 1;
    let mut inQuote: bool = false;
    while r < (line.chars().count() as i64) {
        let c = line.chars().nth((r) as usize).unwrap_or('\0') as i64;
        if inQuote {
            if c == 34 {
                inQuote = false;
            }        } else {
            if c == 34 {
                inQuote = true;
            } else {
                if c == 41 {
                    break;
                }            }
        }

        r += 1;
    }
    if r >= (line.chars().count() as i64) {
        return None;
    }
    let name = line.chars().take((q) as usize).skip((nameStart) as usize).collect::<String>();
    let argstr = line.chars().take((r) as usize).skip((q + 1) as usize).collect::<String>();
    return Some(CompScan { name: name.to_string(), argstr: argstr.to_string(), afterParen: r });
}

fn isContainerCompOpen(line: &str) -> bool {
    let found = compOpenScan(line);
    if found == None {
        return false;
    }
    let cs = found.unwrap_or(CompScan { name: "".to_string(), argstr: "".to_string(), afterParen: 0 });
    if cs.name != "callout" {
        if cs.name != "details" {
            return false;
        }    }
    let mut b: i64 = cs.afterParen + 1;
    while b < (line.chars().count() as i64) {
        if line.chars().nth((b) as usize).unwrap_or('\0') as i64 == 32 {
            b += 1;
        } else {
            break;
        }

    }
    if b >= (line.chars().count() as i64) {
        return false;
    }
    if line.chars().nth((b) as usize).unwrap_or('\0') as i64 != 123 {
        return false;
    }
    let mut t: i64 = b + 1;
    while t < (line.chars().count() as i64) {
        if line.chars().nth((t) as usize).unwrap_or('\0') as i64 != 32 {
            return false;
        }
        t += 1;
    }
    return true;
}

fn leafEndsAfterParen(line: &str, afterParen: i64) -> bool {
    let mut t: i64 = afterParen + 1;
    while t < (line.chars().count() as i64) {
        if line.chars().nth((t) as usize).unwrap_or('\0') as i64 != 32 {
            return false;
        }
        t += 1;
    }
    return true;
}

fn isCompCloseLine(line: &str) -> bool {
    if (line.chars().count() as i64) == 0 {
        return false;
    }
    if line.chars().nth((0) as usize).unwrap_or('\0') as i64 != 125 {
        return false;
    }
    let mut t: i64 = 1;
    while t < (line.chars().count() as i64) {
        if line.chars().nth((t) as usize).unwrap_or('\0') as i64 != 32 {
            return false;
        }
        t += 1;
    }
    return true;
}

fn argValueAt(args: &str, key: &str) -> i64 {
    let want: String = format!("{}{}", key, ":");
    let mut i: i64 = 0;
    let n: i64 = (args.chars().count() as i64);
    while i < n {
        let sc = args.chars().nth((i) as usize).unwrap_or('\0') as i64;
        if sc == 44 {
            i += 1;
        } else {
            if sc == 32 {
                i += 1;
            }        }

        
        let mut m: i64 = i;
        while m < n {
            if args.chars().nth((m) as usize).unwrap_or('\0') as i64 == 32 {
                m += 1;
            } else {
                break;
            }

        }
        if m + (want.chars().count() as i64) <= n {
            if args.chars().take((m + (want.chars().count() as i64)) as usize).skip((m) as usize).collect::<String>() == want {
                let mut v: i64 = m + (want.chars().count() as i64);
                while v < n {
                    if args.chars().nth((v) as usize).unwrap_or('\0') as i64 == 32 {
                        v += 1;
                    } else {
                        break;
                    }

                }
                if v < n {
                    return v;
                }            }        }
        
        let mut inQ: bool = false;
        while i < n {
            let c = args.chars().nth((i) as usize).unwrap_or('\0') as i64;
            if inQ {
                if c == 34 {
                    inQ = false;
                }            } else {
                if c == 34 {
                    inQ = true;
                } else {
                    if c == 44 {
                        break;
                    }                }
            }

            i += 1;
        }
        if i < n {
            i += 1;
        }
    }
    return -1;
}

fn argStrOf(args: &str, key: &str) -> Option<String> {
    let v = argValueAt(args, key);
    if v == -1 {
        return None;
    }
    if args.chars().nth((v) as usize).unwrap_or('\0') as i64 != 34 {
        return None;
    }
    let mut e: i64 = v + 1;
    let n: i64 = (args.chars().count() as i64);
    while e < n {
        if args.chars().nth((e) as usize).unwrap_or('\0') as i64 == 34 {
            break;
        }
        e += 1;
    }
    if e >= n {
        return None;
    }
    return Some(args.chars().take((e) as usize).skip((v + 1) as usize).collect::<String>());
}

fn argBoolOf(args: &str, key: &str) -> Option<bool> {
    let v = argValueAt(args, key);
    if v == -1 {
        return None;
    }
    if args.chars().take((v + 4) as usize).skip((v) as usize).collect::<String>() == "true" {
        return Some(true);
    }
    if args.chars().take((v + 5) as usize).skip((v) as usize).collect::<String>() == "false" {
        return Some(false);
    }
    return None;
}

fn parseBlocks(mut lines: Vec<String>, isFinal: bool) -> Vec<WNode> {
    let mut nodes: Vec<WNode> = vec![];
    let mut i: i64 = 0;
    while i < (lines.len() as i64) {
        let line = lines[(i) as usize].clone();
        if isBlank(line.as_str()) {
            i += 1;
            continue;
        }
        

        let mk = fenceMarker(line.as_str());
        if mk != "" {
            let run = fenceMarkerRun(line.as_str());
            let info = line.trim().to_string()[((run.chars().count() as i64)) as usize..].to_string();
            let language = info.trim().to_string();
            let mut body: Vec<String> = vec![];
            let mut j: i64 = i + 1;
            let mut closed: bool = false;
            while j < (lines.len() as i64) {
                if isCloseFence(lines[(j) as usize].as_str(), mk.as_str(), run.as_str()) {
                    closed = true;
                    break;
                }
                body.push(lines[(j) as usize].clone());
                j += 1;
            }
            let mut code = body.join("\n");
            if closed {
                if (body.len() as i64) > 0 {
                    code += "\n";
                } else {
                    code = "".to_string();
                }
                nodes.push(codeNode(language.as_str(), code.as_str(), false));
            } else {
                


                while (body.len() as i64) > 0 {
                    let tailLine = body[((body.len() as i64) - 1) as usize].clone().trim().to_string();
                    if tailLine == "" {
                        break;
                    }
                    if !(isFenceTailRun(tailLine.as_str())) {
                        break;
                    }
                    body.pop();
                }
                let mut openCode = body.join("\n");
                


                openCode = stripTrailingSpaceLine(openCode.as_str());
                nodes.push(codeNode(language.as_str(), openCode.as_str(), !(isFinal)));
            }
            i = j + 1;
            continue;
        }
        

        let hlevel = headingLevel(line.as_str());
        if hlevel > 0 {
            let t = line.trim().to_string();
            let mut content = t.chars().skip((hlevel) as usize).collect::<String>().trim().to_string();
            let clean = stripTrailingHashes(content.as_str());
            let mut children = parseInline(clean.as_str(), isFinal);
            if clean == "" {
                children = noNodes();
            }            nodes.push(headingNode(hlevel, children.clone()));
            i += 1;
            continue;
        }
        

        if isThematicBreak(line.as_str()) {
            nodes.push(thematicNode());
            i += 1;
            continue;
        }
        

        let comp = compOpenScan(line.as_str());
        if comp != None {
            let mut cs = comp.unwrap_or(CompScan { name: "".to_string(), argstr: "".to_string(), afterParen: 0 });
            if isContainerCompOpen(line.as_str()) {
                



                let mut body: Vec<String> = vec![];
                let mut j: i64 = i + 1;
                let mut depth: i64 = 1;
                let mut compClosed: bool = false;
                while j < (lines.len() as i64) {
                    if isCompCloseLine(lines[(j) as usize].as_str()) {
                        depth -= 1;
                        if depth == 0 {
                            compClosed = true;
                            break;
                        }                    } else {
                        if isContainerCompOpen(lines[(j) as usize].as_str()) {
                            depth += 1;
                        }                    }

                    body.push(lines[(j) as usize].clone());
                    j += 1;
                }
                if compClosed {
                    let inner = parseBlocks(body.clone(), isFinal);
                    if cs.name == "callout" {
                        let ctitle: String = argStrOf(cs.argstr.as_str(), "title").unwrap_or("".to_string());
                        nodes.push(calloutNode(argStrOf(cs.argstr.as_str(), "type").unwrap_or("".to_string()).as_str(), ctitle.as_str(), inner.clone()));
                    } else {
                        let dopen: bool = argBoolOf(cs.argstr.as_str(), "open").unwrap_or(false);
                        nodes.push(detailsNode(argStrOf(cs.argstr.as_str(), "summary").unwrap_or("".to_string()).as_str(), dopen, inner.clone()));
                    }
                    i = j + 1;
                    continue;
                }                

            } else {
                if cs.name == "query" {
                    if leafEndsAfterParen(line.as_str(), cs.afterParen) {
                        nodes.push(queryNode(cs.argstr.trim()));
                        i += 1;
                        continue;
                    }                }                if cs.name == "embed" {
                    if leafEndsAfterParen(line.as_str(), cs.afterParen) {
                        nodes.push(embedNode(argStrOf(cs.argstr.as_str(), "src").unwrap_or("".to_string()).as_str()));
                        i += 1;
                        continue;
                    }                }            }
        }
        

        if startsBlockquote(line.as_str()) {
            let mut qlines: Vec<String> = vec![];
            let mut j: i64 = i;
            while j < (lines.len() as i64) {
                if startsBlockquote(lines[(j) as usize].as_str()) {
                    qlines.push(quoteBody(lines[(j) as usize].as_str()));
                    j += 1;
                } else {
                    if isBlank(lines[(j) as usize].as_str()) {
                        break;
                    }                    

                    if isParagraphStart(lines[(j) as usize].as_str()) {
                        qlines.push(lines[(j) as usize].clone());
                        j += 1;
                    } else {
                        break;
                    }
                }

            }
            let inner = parseBlocks(qlines.clone(), isFinal);
            nodes.push(quoteNode(inner.clone()));
            i = j;
            continue;
        }
        

        if isTableRow(line.as_str()) {
            if i + 1 < (lines.len() as i64) {
                if isTableDelimiter(lines[(i + 1) as usize].as_str()) {
                    



                    let headCount: i64 = (splitRowCells(line.as_str()).len() as i64);
                    let delimCount: i64 = (splitRowCells(lines[(i + 1) as usize].as_str()).len() as i64);
                    if headCount == delimCount {
                        let mut tableEnd = tableConsume(lines.clone(), i, &mut nodes, isFinal);
                        i = tableEnd;
                        continue;
                    }                }            }        }
        

        let bnum = olMarkerNum(line.as_str());
        let bmark = bulletMarker(line.as_str());
        if bnum >= 0 {
            let mut listEnd = parseList(lines.clone(), i, true, bnum, &mut nodes, isFinal);
            i = listEnd;
            continue;
        }
        if bmark != "" {
            let mut listEnd2 = parseList(lines.clone(), i, false, 0, &mut nodes, isFinal);
            i = listEnd2;
            continue;
        }
        if bulletMarkerBare(line.as_str()) {
            

            let mut bareEnd = parseList(lines.clone(), i, false, 0, &mut nodes, isFinal);
            i = bareEnd;
            continue;
        }
        

        let mut para: Vec<String> = vec![];
        let mut j: i64 = i;
        let mut setextLevel: i64 = 0;
        while j < (lines.len() as i64) {
            let cur = lines[(j) as usize].clone();
            if isBlank(cur.as_str()) {
                break;
            }
            if (para.len() as i64) > 0 {
                let su = isSetextUnderline(cur.as_str());
                if su > 0 {
                    setextLevel = su;
                    j += 1;
                    break;
                }            }
            if paraBreaks(cur.as_str(), lines.clone(), j) {
                break;
            }
            para.push(stripParaIndent(cur.as_str()));
            j += 1;
        }
        if setextLevel > 0 {
            let mut content = para.join("\n");
            let children = parseInline(content.as_str(), isFinal);
            nodes.push(headingNode(setextLevel, children.clone()));
            i = j;
            continue;
        }
        if (para.len() as i64) > 0 {
            



            if !(isFinal) {
                let mut preOk: bool = false;
                if (para.len() as i64) >= 2 {
                    preOk = true;
                }                if j < (lines.len() as i64) {
                    preOk = true;
                }                if preOk {
                    let head = para[0].clone();
                    if isTableRow(head.as_str()) {
                        if endsWithTokSeq(head.trim(), "|") {
                            let preCells = splitRowCells(head.as_str());
                            if (preCells.len() as i64) >= 2 {
                                let mut allPipes: bool = true;
                                let mut pi: i64 = 0;
                                while pi < (para.len() as i64) {
                                    let pl = para[(pi) as usize].clone().trim().to_string();
                                    if !(startsWithStr(pl.as_str(), "|")) {
                                        allPipes = false;
                                    }
                                    pi += 1;
                                }
                                if allPipes {
                                    let mut preRow: Vec<WNode> = vec![];
                                    for pc in &preCells {
                                        let pt = pc.trim().to_string();
                                        let pChildren = parseInline(pt.as_str(), isFinal);
                                        preRow.push(cellNode(true, pChildren.clone(), "left"));
                                    }
                                    let preHeader = rowNode(preRow.clone());
                                    nodes.push(tableNode(vec![preHeader.clone()], noNodes(), true));
                                    i = j;
                                    continue;
                                }                            }                        }                    }                }            }            let content = para.join("\n");
            let children = parseInline(content.as_str(), isFinal);
            nodes.push(paraNode(children.clone()));
            i = j;
            continue;
        }
        i += 1;
    }
    return nodes;
}

fn paraBreaks(cur: &str, lines: Vec<String>, idx: i64) -> bool {
    if idx == 0 {
        return false;
    }
    if fenceMarker(cur) != "" {
        return true;
    }
    if headingLevel(cur) > 0 {
        return true;
    }
    if isThematicBreak(cur) {
        return true;
    }
    if startsBlockquote(cur) {
        return true;
    }
    if bulletMarker(cur) != "" {
        return true;
    }
    if bulletMarkerBare(cur) {
        return true;
    }
    if olMarkerNum(cur) >= 0 {
        return true;
    }
    if compOpenScan(cur) != None {
        return true;
    }
    return false;
}

fn isParagraphStart(line: &str) -> bool {
    if paraBreaks(line, vec![], 0) {
        return false;
    }
    return true;
}

fn parseList(mut lines: Vec<String>, start: i64, ordered: bool, firstNum: i64, mut nodes: &mut Vec<WNode>, isFinal: bool) -> i64 {
    let mut items: Vec<WNode> = vec![];
    let mut i: i64 = start;
    let mut startN: Option<i64> = None;
    if ordered {
        if firstNum != 1 {
            startN = Some(firstNum);
        }    }
    while i < (lines.len() as i64) {
        let line = lines[(i) as usize].clone();
        if isBlank(line.as_str()) {
            

            let mut k: i64 = i + 1;
            while k < (lines.len() as i64) {
                if isBlank(lines[(k) as usize].as_str()) {
                    k += 1;
                } else {
                    break;
                }

            }
            if k < (lines.len() as i64) {
                if bulletMarker(lines[(k) as usize].as_str()) != "" {
                    i = k;
                    continue;
                }                if bulletMarkerBare(lines[(k) as usize].as_str()) {
                    i = k;
                    continue;
                }                if olMarkerNum(lines[(k) as usize].as_str()) >= 0 {
                    i = k;
                    continue;
                }            }            break;
        }
        
        let mut markerIndent: i64 = 0;
        let mut contentStart: i64 = 0;
        let mut isItem: bool = false;
        let mut orderedHere: bool = false;
        let bm = bulletMarker(line.as_str());
        let om = olMarkerNum(line.as_str());
        if bm != "" {
            markerIndent = indentOf(line.as_str());
            contentStart = 2 + indentOf(line.as_str());
            isItem = true;
            orderedHere = false;
        } else {
            if bulletMarkerBare(line.as_str()) {
                markerIndent = indentOf(line.as_str());
                contentStart = 1 + indentOf(line.as_str());
                isItem = true;
                orderedHere = false;
            } else {
                if om >= 0 {
                    markerIndent = indentOf(line.as_str());
                    let t = trimStartStr(line.as_str());
                    


                    let mut q: i64 = 0;
                    while q < (t.chars().count() as i64) {
                        let dc = t.chars().nth((q) as usize).unwrap_or('\0') as i64;
                        if dc >= 48 {
                            if dc <= 57 {
                                q += 1;
                            } else {
                                break;
                            }
                        } else {
                            break;
                        }

                    }
                    let mut markerWidth: i64 = q + 1;
                    let mut spaces: i64 = 0;
                    let mut p2: i64 = q + 1;
                    while p2 < (t.chars().count() as i64) {
                        if t.chars().nth((p2) as usize).unwrap_or('\0') as i64 == 32 {
                            spaces += 1;
                            p2 += 1;
                        } else {
                            break;
                        }

                    }
                    if spaces > 4 {
                        spaces = 1;
                    }                    if spaces < 1 {
                        spaces = 1;
                    }                    contentStart = markerIndent + markerWidth + spaces;
                    isItem = true;
                    orderedHere = true;
                }            }
        }

        
        if !(isItem) {
            break;
        }
        if orderedHere != ordered {
            break;
        }
        if !(ordered) {
            

            let t = trimStartStr(line.as_str());
            let firstChar = t.chars().take((1) as usize).skip((0) as usize).collect::<String>();
            let firstItem = trimStartStr(lines[(start) as usize].as_str());
            let listChar = firstItem.chars().take((1) as usize).skip((0) as usize).collect::<String>();
            if firstChar != listChar {
                break;
            }        }
        

        let mut itemLines: Vec<String> = vec![];
        let mut firstText = line.chars().skip((contentStart) as usize).collect::<String>();
        itemLines.push(firstText.to_string());
        i += 1;
        while i < (lines.len() as i64) {
            let cur = lines[(i) as usize].clone();
            if isBlank(cur.as_str()) {
                

                let mut k: i64 = i + 1;
                while k < (lines.len() as i64) {
                    if isBlank(lines[(k) as usize].as_str()) {
                        k += 1;
                    } else {
                        break;
                    }

                }
                if k < (lines.len() as i64) {
                    if indentWidth(lines[(k) as usize].as_str()) >= contentStart {
                        if paraBreaks(lines[(k) as usize].as_str(), lines.clone(), k) {
                            

                        } else {
                            

                        }
                        let mut bi: i64 = i;
                        while bi < k {
                            itemLines.push("".to_string());
                            bi += 1;
                        }
                        i = k;
                        continue;
                    }                }                break;
            }
            if indentWidth(cur.as_str()) >= contentStart {
                let mut dedent = cur.chars().skip((contentStart) as usize).collect::<String>();
                itemLines.push(dedent.to_string());
                i += 1;
                continue;
            }
            
            if !(paraBreaks(cur.as_str(), lines.clone(), i)) {
                itemLines.push(cur.to_string());
                i += 1;
                continue;
            }
            break;
        }
        let itemNodes = parseBlocks(itemLines.clone(), isFinal);
        items.push(itemNode(itemNodes.clone()));
    }
    if ordered {
        if startN != None {
            nodes.push(listNode(true, startN.clone(), items.clone()));
        } else {
            nodes.push(listNode(true, None, items.clone()));
        }
    } else {
        nodes.push(listNode(false, None, items.clone()));
    }

    return i;
}

fn tableConsume(mut lines: Vec<String>, start: i64, mut nodes: &mut Vec<WNode>, isFinal: bool) -> i64 {
    let headerCells = splitRowCells(lines[(start) as usize].as_str());
    let mut aligns: Vec<String> = vec![];
    let delimCells = splitRowCells(lines[(start + 1) as usize].as_str());
    for c in &delimCells {
        aligns.push(delimiterAlign(c.as_str()));
    }
    let mut rows: Vec<WNode> = vec![];
    let mut i: i64 = start + 2;
    while i < (lines.len() as i64) {
        if !(isTableRow(lines[(i) as usize].as_str())) {
            break;
        }
        let cells = splitRowCells(lines[(i) as usize].as_str());
        let mut rowCells: Vec<WNode> = vec![];
        let mut ci: i64 = 0;
        while ci < (headerCells.len() as i64) {
            let mut cellText: String = "".to_string();
            if ci < (cells.len() as i64) {
                cellText = cells[(ci) as usize].clone().trim().to_string();
            }
            let mut align: String = "left".to_string();
            if ci < (aligns.len() as i64) {
                align = aligns[(ci) as usize].clone();
            }
            let mut children = parseInline(cellText.as_str(), isFinal);
            if cellText == "" {
                children = noNodes();
            }
            rowCells.push(cellNode(false, children.clone(), align.as_str()));
            ci += 1;
        }
        rows.push(rowNode(rowCells.clone()));
        i += 1;
    }
    let mut headerRowCells: Vec<WNode> = vec![];
    let mut ci: i64 = 0;
    while ci < (headerCells.len() as i64) {
        let cellText = headerCells[(ci) as usize].clone().trim().to_string();
        let mut align: String = "left".to_string();
        if ci < (aligns.len() as i64) {
            align = aligns[(ci) as usize].clone();
        }
        let children = parseInline(cellText.as_str(), isFinal);
        headerRowCells.push(cellNode(true, children.clone(), align.as_str()));
        ci += 1;
    }
    let header = rowNode(headerRowCells.clone());
    nodes.push(tableNode(vec![header.clone()], rows.clone(), false));
    return i;
}

fn parseInline(text: &str, isFinal: bool) -> Vec<WNode> {
    if text == "" {
        return noNodes();
    }
    return parseInlineLine(text, isFinal);
}

fn parseInlineLine(line: &str, isFinal: bool) -> Vec<WNode> {
    let mut nodes: Vec<WNode> = vec![];
    let mut buf: String = "".to_string();
    let mut i: i64 = 0;
    let mut seenCode: bool = false;


    let lineLen: i64 = (line.chars().count() as i64);
    while i < lineLen {
        let cs = line.chars().take((i + 1) as usize).skip((i) as usize).collect::<String>();
        

        if cs == "\n" {
            let mut sp: i64 = 0;
            while sp < (buf.chars().count() as i64) {
                if buf.chars().nth(((buf.chars().count() as i64) - 1 - sp) as usize).unwrap_or('\0') as i64 == 32 {
                    sp += 1;
                } else {
                    break;
                }

            }
            if sp >= 2 {
                let kept = buf.chars().take(((buf.chars().count() as i64) - sp) as usize).skip((0) as usize).collect::<String>();
                if kept != "" {
                    nodes.push(textNode(kept.as_str()));
                }                nodes.push(hardbreakNode());
                buf = "".to_string();
            } else {
                if sp > 0 {
                    buf = buf.chars().take(((buf.chars().count() as i64) - sp) as usize).skip((0) as usize).collect::<String>();
                }                buf += &"\n";
            }
            i += 1;
            continue;
        }
        

        if cs == "*" {
            



            if startsWithAt(line, "***", i) {
                let mut afterT = scanDelim(line, i, "***", false, isFinal);
                if afterT != None {
                    let scT = afterT.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(strongNode(vec![emNode(parseInlineLine(scT.inner.as_str(), isFinal))]));
                    i = scT.next;
                    continue;
                }            }            if startsWithAt(line, "**", i) {
                let mut after = scanDelim(line, i, "**", true, isFinal);
                if after != None {
                    let sc = after.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(strongNode(parseInlineLine(sc.inner.as_str(), isFinal)));
                    i = sc.next;
                    continue;
                }            }            let mut afterEm = scanDelim(line, i, "*", false, isFinal);
            if afterEm != None {
                let scEm = afterEm.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                if buf != "" {
                    nodes.push(textNode(buf.as_str()));
                    buf = "".to_string();
                }                nodes.push(emNode(parseInlineLine(scEm.inner.as_str(), isFinal)));
                i = scEm.next;
                continue;
            }            buf += &cs;
            i += 1;
            continue;
        }
        if cs == "_" {
            


            if startsWithAt(line, "___", i) {
                let mut afterU3 = scanDelim(line, i, "___", false, isFinal);
                if afterU3 != None {
                    let scU3 = afterU3.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(underlineNode(vec![emNode(parseInlineLine(scU3.inner.as_str(), isFinal))]));
                    i = scU3.next;
                    continue;
                }            }            


            if startsWithAt(line, "__", i) {
                let mut afterU2 = scanDelim(line, i, "__", false, isFinal);
                if afterU2 != None {
                    let scU2 = afterU2.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(underlineNode(parseInlineLine(scU2.inner.as_str(), isFinal)));
                    i = scU2.next;
                    continue;
                }            }            let mut afterU = scanDelim(line, i, "_", false, isFinal);
            if afterU != None {
                let scU = afterU.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                if buf != "" {
                    nodes.push(textNode(buf.as_str()));
                    buf = "".to_string();
                }                nodes.push(emNode(parseInlineLine(scU.inner.as_str(), isFinal)));
                i = scU.next;
                continue;
            }            buf += &cs;
            i += 1;
            continue;
        }
        if cs == "~" {
            if startsWithAt(line, "~~", i) {
                let mut afterS = scanDelim(line, i, "~~", false, isFinal);
                if afterS != None {
                    let scS = afterS.unwrap_or(DelimScan { next: 0, inner: "".to_string() });
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(strikeNode(parseInlineLine(scS.inner.as_str(), isFinal)));
                    i = scS.next;
                    continue;
                }            }            buf += &cs;
            i += 1;
            continue;
        }
        

        if cs == "`" {
            let mut run: i64 = 0;
            while startsWithAt(line, "`", i + run) {
                run += 1;
            }
            let mut close = findBacktickRun(line, i + run, run);
            if close != -1 {
                let mut inner = line.chars().take((close) as usize).skip((i + run) as usize).collect::<String>();
                if startsWithStr(inner.as_str(), " ") {
                    if endsWithTokSeq(inner.as_str(), " ") {
                        if inner.trim().to_string() != "" {
                            inner = inner.chars().take(((inner.chars().count() as i64) - 1) as usize).skip((1) as usize).collect::<String>();
                        }                    }                }                if buf != "" {
                    nodes.push(textNode(buf.as_str()));
                    buf = "".to_string();
                }                nodes.push(codeSpanNode(inner.as_str()));
                seenCode = true;
                i = close + run;
                continue;
            }            


            if !(isFinal) {
                if run == 1 {
                    let restAll = line.chars().skip((i + run) as usize).collect::<String>();
                    if restAll.trim().to_string() == "" {
                        if buf == "" {
                            i = (line.chars().count() as i64);
                            continue;
                        }                    }                }            }            if run == 1 {
                if !(isFinal) {
                    


                    let rest = line.chars().skip((i + 1) as usize).collect::<String>();
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(codeSpanNode(rest.as_str()));
                    seenCode = true;
                    i = (line.chars().count() as i64);
                    continue;
                }            }            


            buf += &line.chars().take((i + run) as usize).skip((i) as usize).collect::<String>();
            i += run;
            continue;
        }
        

        if cs == "!" {
            if startsWithAt(line, "![", i) {
                let mut imgAfter = scanLink(line, i + 1, isFinal, seenCode);
                if imgAfter != None {
                    let img = imgAfter.unwrap_or(LinkScan { next: 0, text: "".to_string(), href: "".to_string(), loading: false, title: None, tail: "".to_string() });
                    if buf != "" {
                        nodes.push(textNode(buf.as_str()));
                        buf = "".to_string();
                    }                    nodes.push(imageNode(img.href.as_str(), img.text.as_str()));
                    i = img.next;
                    continue;
                }            }            buf += &cs;
            i += 1;
            continue;
        }
        

        if cs == "[" {
            let mut after = scanLink(line, i, isFinal, seenCode);
            if after != None {
                let lk = after.unwrap_or(LinkScan { next: 0, text: "".to_string(), href: "".to_string(), loading: false, title: None, tail: "".to_string() });
                if buf != "" {
                    nodes.push(textNode(buf.as_str()));
                    buf = "".to_string();
                }                if lk.loading {
                    

                    nodes.push(linkNode(lk.href.as_str(), lk.title.clone(), lk.text.as_str(), parseInlineLine(lk.text.as_str(), isFinal), true));
                    if lk.tail != "" {
                        nodes.push(textNode(lk.tail.as_str()));
                    }                } else {
                    nodes.push(linkNode(lk.href.as_str(), lk.title.clone(), lk.text.as_str(), parseInlineLine(lk.text.as_str(), isFinal), false));
                }
                i = lk.next;
                continue;
            }            buf += &cs;
            i += 1;
            continue;
        }
        


        if cs == "\\" {
            if i + 1 < (line.chars().count() as i64) {
                let nc = line.chars().nth((i + 1) as usize).unwrap_or('\0') as i64;
                if isPunctuationCode(nc) {
                    if nc == 34 {
                        buf += &char::from_u32((1) as u32).unwrap_or('\u{0}').to_string();
                    } else {
                        if nc == 39 {
                            buf += &char::from_u32((2) as u32).unwrap_or('\u{0}').to_string();
                        } else {
                            buf += &line.chars().take((i + 2) as usize).skip((i + 1) as usize).collect::<String>();
                        }
                    }
                    i += 2;
                    continue;
                }            }        }
        
        buf += &cs;
        i += 1;
    }
    if buf != "" {
        nodes.push(textNode(buf.as_str()));
    }
    if !(isFinal) {
        trimStreamingTail(nodes.clone());
    }
    return nodes;
}

fn trimStreamingTail(mut nodes: Vec<WNode>) {
    if (nodes.len() as i64) > 0 {
        let last = nodes[((nodes.len() as i64) - 1) as usize].clone();
        if last.r#type == "text" {
            trimLastTextNode(last.clone(), nodes.clone());
        }    }
}

fn stripTrailingPartialTag(s: &str) -> String {
    let mut lastGt: i64 = -1;
    let mut i: i64 = 0;
    while i < (s.chars().count() as i64) {
        if s.chars().nth((i) as usize).unwrap_or('\0') as i64 == 62 {
            lastGt = i;
        }
        i += 1;
    }
    let mut j: i64 = lastGt + 1;
    while j < (s.chars().count() as i64) {
        if s.chars().nth((j) as usize).unwrap_or('\0') as i64 == 60 {
            let mut p: i64 = j + 1;
            if p >= (s.chars().count() as i64) {
                return s.to_string();
            }            let mut c = s.chars().nth((p) as usize).unwrap_or('\0') as i64;
            if c == 47 {
                p += 1;
                if p >= (s.chars().count() as i64) {
                    return s.to_string();
                }                c = s.chars().nth((p) as usize).unwrap_or('\0') as i64;
            }            let mut isLetter: bool = false;
            if c == 33 {
                isLetter = true;
            } else {
                if c >= 65 {
                    if c <= 90 {
                        isLetter = true;
                    }                }                if c >= 97 {
                    if c <= 122 {
                        isLetter = true;
                    }                }            }
            if !(isLetter) {
                return s.to_string();
            }            let mut q: i64 = p + 1;
            let mut clean: bool = true;
            while q < (s.chars().count() as i64) {
                if s.chars().nth((q) as usize).unwrap_or('\0') as i64 == 62 {
                    clean = false;
                    break;
                }
                q += 1;
            }
            if !(clean) {
                return s.to_string();
            }            let mut cut: i64 = j;
            if j > 0 {
                if s.chars().nth((j - 1) as usize).unwrap_or('\0') as i64 == 32 {
                    cut = j - 1;
                }            }            return s.chars().take((cut) as usize).skip((0) as usize).collect::<String>().to_string();
        }
        j += 1;
    }
    return s.to_string();
}

fn stripTrailingOpenParens(s: &str) -> String {
    let mut end: i64 = (s.chars().count() as i64);
    while end > 0 {
        let c = s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64;
        if c == 32 {
            end -= 1;
        } else {
            if c == 9 {
                end -= 1;
            } else {
                if c == 10 {
                    end -= 1;
                } else {
                    if c == 13 {
                        end -= 1;
                    } else {
                        break;
                    }
                }
            }
        }

    }
    let mut start: i64 = end;
    while start > 0 {
        if s.chars().nth((start - 1) as usize).unwrap_or('\0') as i64 == 40 {
            start -= 1;
        } else {
            break;
        }

    }
    if start == end {
        return s.to_string();
    }
    return s.chars().take((start) as usize).skip((0) as usize).collect::<String>().to_string();
}

fn stripTrailingStarSpaces(s: &str) -> String {
    let mut end: i64 = (s.chars().count() as i64);
    while end > 0 {
        if s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64 == 32 {
            end -= 1;
        } else {
            break;
        }

    }
    if end == (s.chars().count() as i64) {
        return s.to_string();
    }
    if end == 0 {
        return s.to_string();
    }
    if s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64 == 42 {
        return s.chars().take((end - 1) as usize).skip((0) as usize).collect::<String>().to_string();
    }
    return s.to_string();
}

fn stripTrailingSpaces(s: &str) -> String {
    let mut end: i64 = (s.chars().count() as i64);
    while end > 0 {
        if s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64 == 32 {
            end -= 1;
        } else {
            break;
        }

    }
    if end == (s.chars().count() as i64) {
        return s.to_string();
    }
    return s.chars().take((end) as usize).skip((0) as usize).collect::<String>().to_string();
}

fn trimLastTextNode(last: WNode, mut nodes: Vec<WNode>) {
    let mut c: String = last.content.unwrap_or("".to_string());
    let mut stripped: bool = false;
    let mut c2 = stripTrailingPartialTag(c.as_str());
    if c2 == c {
        

        if endsWithTokSeq(c.as_str(), "<") {
            c2 = c.chars().take(((c.chars().count() as i64) - 1) as usize).skip((0) as usize).collect::<String>();
        }    }
    if c2 != c {
        stripped = true;
    }
    c = c2.to_string();
    let mut c3 = stripTrailingOpenParens(c.as_str());
    if c3 != c {
        stripped = true;
    }
    c = c3.to_string();
    let mut c4 = stripTrailingStarSpaces(c.as_str());
    if c4 == c {
        if endsWithTokSeq(c.as_str(), "*") {
            if !(endsWithTokSeq(c.as_str(), "**")) {
                c4 = c.chars().take(((c.chars().count() as i64) - 1) as usize).skip((0) as usize).collect::<String>();
            }        }    }
    if c4 != c {
        stripped = true;
    }
    c = c4.to_string();
    if !(stripped) {
        c = stripTrailingSpaces(c.as_str());
    }
    if c.trim().to_string() == "|" {
        

        c = "".to_string();
    }
    if c == "" {
        nodes.pop();
    } else {
        nodes.pop();
        nodes.push(rawTextNode(c.as_str()));
    }

}

pub fn textNode(content: &str) -> WNode {
    let mut s = smartQuotes(content);

    s = s.split(&char::from_u32((1) as u32).unwrap_or('\u{0}').to_string()).map(|s| s.to_string()).collect::<Vec<String>>().join("\"");
    s = s.split(&char::from_u32((2) as u32).unwrap_or('\u{0}').to_string()).map(|s| s.to_string()).collect::<Vec<String>>().join("'");
    return WNode { r#type: "text".to_string(), content: Some(s), level: None, language: None, code: None, loading: None, children: None, ordered: None, start: None, items: None, cells: None, header: None, rows: None, isHeader: None, align: None, href: None, title: None, text: None, src: None, alt: None, checked: None };
}

fn isWordCharCode(c: i64) -> bool {
    if c >= 48 {
        if c <= 57 {
            return true;
        }    }
    if c >= 65 {
        if c <= 90 {
            return true;
        }    }
    if c >= 97 {
        if c <= 122 {
            return true;
        }    }
    if c == 95 {
        return true;
    }
    return false;
}

fn isClosePunctCode(c: i64) -> bool {
    if c == 41 {
        return true;
    }
    if c == 93 {
        return true;
    }
    if c == 125 {
        return true;
    }
    if c == 44 {
        return true;
    }
    if c == 46 {
        return true;
    }
    if c == 59 {
        return true;
    }
    if c == 58 {
        return true;
    }
    if c == 33 {
        return true;
    }
    if c == 63 {
        return true;
    }
    if c == 8230 {
        return true;
    }
    if c == 34 {
        return true;
    }
    if c == 39 {
        return true;
    }
    if c == 65289 {
        return true;
    }
    if c == 65292 {
        return true;
    }
    if c == 65294 {
        return true;
    }
    if c == 12290 {
        return true;
    }
    if c == 65307 {
        return true;
    }
    if c == 65306 {
        return true;
    }
    if c == 65301 {
        return true;
    }
    if c == 65311 {
        return true;
    }
    if c == 12301 {
        return true;
    }
    if c == 12303 {
        return true;
    }
    if c == 12313 {
        return true;
    }
    if c == 12311 {
        return true;
    }
    return false;
}

fn isPunctuationCode(c: i64) -> bool {
    if c >= 33 {
        if c <= 47 {
            return true;
        }    }
    if c >= 58 {
        if c <= 64 {
            return true;
        }    }
    if c >= 91 {
        if c <= 96 {
            return true;
        }    }
    if c >= 123 {
        if c <= 126 {
            return true;
        }    }
    if c == 161 {
        return true;
    }
    if c == 167 {
        return true;
    }
    if c == 171 {
        return true;
    }
    if c == 182 {
        return true;
    }
    if c == 183 {
        return true;
    }
    if c == 191 {
        return true;
    }
    if c >= 8208 {
        if c <= 8286 {
            return true;
        }    }
    if c >= 12288 {
        if c <= 12351 {
            return true;
        }    }
    if c >= 65281 {
        if c <= 65380 {
            return true;
        }    }
    return false;
}

fn CURLY_LDQUO() -> String {
    return char::from_u32((8220) as u32).unwrap_or('\u{0}').to_string();
}

fn CURLY_RDQUO() -> String {
    return char::from_u32((8221) as u32).unwrap_or('\u{0}').to_string();
}

fn CURLY_LSQUO() -> String {
    return char::from_u32((8216) as u32).unwrap_or('\u{0}').to_string();
}

fn CURLY_RSQUO() -> String {
    return char::from_u32((8217) as u32).unwrap_or('\u{0}').to_string();
}

fn smartQuotes(s: &str) -> String {
    let mut out: String = "".to_string();
    let mut i: i64 = 0;
    let sLen: i64 = (s.chars().count() as i64);
    while i < sLen {
        let cs = s.chars().take((i + 1) as usize).skip((i) as usize).collect::<String>();
        if cs == "\"" {
            let mut prevIsOpenCtx: bool = false;
            if out == "" {
                prevIsOpenCtx = true;
            } else {
                let prev = out.chars().take(((out.chars().count() as i64)) as usize).skip(((out.chars().count() as i64) - 1) as usize).collect::<String>();
                if prev == " " {
                    prevIsOpenCtx = true;
                }                if prev == "\n" {
                    prevIsOpenCtx = true;
                }                if prev == "(" {
                    prevIsOpenCtx = true;
                }                if prev == "[" {
                    prevIsOpenCtx = true;
                }                if prev == "{" {
                    prevIsOpenCtx = true;
                }            }
            if prevIsOpenCtx {
                out += &CURLY_LDQUO();
                i += 1;
                continue;
            }            

            if i + 1 >= (s.chars().count() as i64) {
                out += &CURLY_RDQUO();
                i += 1;
                continue;
            }            let nc = s.chars().nth((i + 1) as usize).unwrap_or('\0') as i64;
            if nc == 32 {
                out += &CURLY_RDQUO();
            } else {
                if nc == 10 {
                    out += &CURLY_RDQUO();
                } else {
                    if isClosePunctCode(nc) {
                        out += &CURLY_RDQUO();
                    } else {
                        out += &cs;
                    }
                }
            }
            i += 1;
            continue;
        }
        if cs == "'" {
            let mut apostrophe: bool = false;
            if i > 0 {
                if i + 1 < (s.chars().count() as i64) {
                    let pc = s.chars().nth((i - 1) as usize).unwrap_or('\0') as i64;
                    let ncc = s.chars().nth((i + 1) as usize).unwrap_or('\0') as i64;
                    let pWord = isWordCharCode(pc);
                    let nWord = isWordCharCode(ncc);
                    if pWord {
                        if nWord {
                            apostrophe = true;
                        }                    }                }            }            if apostrophe {
                out += &CURLY_RSQUO();
            } else {
                let mut openS: bool = false;
                if out == "" {
                    openS = true;
                } else {
                    let prev2 = out.chars().take(((out.chars().count() as i64)) as usize).skip(((out.chars().count() as i64) - 1) as usize).collect::<String>();
                    if prev2 == " " {
                        openS = true;
                    }                    if prev2 == "\n" {
                        openS = true;
                    }                    if prev2 == "(" {
                        openS = true;
                    }                    if prev2 == "[" {
                        openS = true;
                    }                }
                if openS {
                    out += &CURLY_LSQUO();
                } else {
                    out += &CURLY_RSQUO();
                }
            }
            i += 1;
            continue;
        }
        out += &cs;
        i += 1;
    }
    return out;
}

fn isNestingDelimCode(c: i64) -> bool {
    if c == 42 {
        return true;
    }
    return c == 95;
}

fn scanDelim(line: &str, i: i64, delim: &str, autoCloseWhenFinal: bool, isFinal: bool) -> Option<DelimScan> {



    if delim == "_" {
        if i > 0 {
            let pc = line.chars().nth((i - 1) as usize).unwrap_or('\0') as i64;
            if isWordCharCode(pc) {
                return None;
            }        }    }
    if delim == "__" {
        if i > 0 {
            let pc2 = line.chars().nth((i - 1) as usize).unwrap_or('\0') as i64;
            if isWordCharCode(pc2) {
                return None;
            }        }    }
    if delim == "___" {
        if i > 0 {
            let pc3 = line.chars().nth((i - 1) as usize).unwrap_or('\0') as i64;
            if isWordCharCode(pc3) {
                return None;
            }        }    }
    let afterStart: i64 = i + (delim.chars().count() as i64);
    let inner = line.chars().skip((afterStart) as usize).collect::<String>();
    let mut close = findStr(inner.as_str(), delim);
    let mut innerText = inner.clone();
    if close != -1 {
        innerText = inner.chars().take((close) as usize).skip((0) as usize).collect::<String>();
    }
    if innerText == "" {
        

        return None;
    }
    if isPunctuationCode(innerText.chars().nth((0) as usize).unwrap_or('\0') as i64) {
        






        let mut nest: bool = false;
        if close != -1 {
            if isNestingDelimCode(innerText.chars().nth((0) as usize).unwrap_or('\0') as i64) {
                nest = true;
            }        }        if !(nest) {
            return None;
        }    }
    if close != -1 {
        let mut next: i64 = afterStart + close + (delim.chars().count() as i64);
        return Some(DelimScan { next: next, inner: innerText.to_string() });
    }
    if !(autoCloseWhenFinal) {
        if isFinal {
            return None;
        }    }

    if inner == "" {
        return None;
    }
    if inner.chars().nth((0) as usize).unwrap_or('\0') as i64 == 32 {
        return None;
    }
    return Some(DelimScan { next: (line.chars().count() as i64), inner: inner.to_string() });
}

fn findBacktickRun(line: &str, from: i64, count: i64) -> i64 {
    let mut i: i64 = from;
    while i < (line.chars().count() as i64) {
        if line.chars().nth((i) as usize).unwrap_or('\0') as i64 != 96 {
            i += 1;
            continue;
        }
        let mut run: i64 = 0;
        while startsWithAt(line, "`", i + run) {
            run += 1;
        }
        if run == count {
            return i;
        }
        i += run;
    }
    return -1;
}

fn stripTrailingUrlPunct(s: &str) -> String {
    let mut end: i64 = (s.chars().count() as i64);
    while end > 0 {
        let c = s.chars().nth((end - 1) as usize).unwrap_or('\0') as i64;
        if c == 46 {
            end -= 1;
        } else {
            if c == 44 {
                end -= 1;
            } else {
                if c == 58 {
                    end -= 1;
                } else {
                    if c == 59 {
                        end -= 1;
                    } else {
                        if c == 33 {
                            end -= 1;
                        } else {
                            if c == 63 {
                                end -= 1;
                            } else {
                                if c == 41 {
                                    end -= 1;
                                } else {
                                    break;
                                }
                            }
                        }
                    }
                }
            }
        }

    }
    if end == (s.chars().count() as i64) {
        return s.to_string();
    }
    return s.chars().take((end) as usize).skip((0) as usize).collect::<String>().to_string();
}

fn isHttpUrl(s: &str) -> bool {
    if startsWithStr(s, "http://") {
        if (s.chars().count() as i64) > 7 {
            return true;
        }    }
    if startsWithStr(s, "https://") {
        if (s.chars().count() as i64) > 8 {
            return true;
        }    }
    return false;
}

fn isBareDomain(s: &str) -> bool {
    if (s.chars().count() as i64) == 0 {
        return false;
    }
    let mut dot: i64 = -1;
    let mut i: i64 = 0;
    while i < (s.chars().count() as i64) {
        let c = s.chars().nth((i) as usize).unwrap_or('\0') as i64;
        let mut ok: bool = false;
        if c >= 48 {
            if c <= 57 {
                ok = true;
            }        }
        if c >= 65 {
            if c <= 90 {
                ok = true;
            }        }
        if c >= 97 {
            if c <= 122 {
                ok = true;
            }        }
        if c == 45 {
            ok = true;
        }
        if c == 46 {
            ok = true;
            dot = i;
        }
        if !(ok) {
            return false;
        }
        i += 1;
    }
    if dot <= 0 {
        return false;
    }
    let mut letters: i64 = 0;
    let mut j: i64 = dot + 1;
    while j < (s.chars().count() as i64) {
        let c = s.chars().nth((j) as usize).unwrap_or('\0') as i64;
        if c >= 65 {
            if c <= 90 {
                letters += 1;
            } else {
                return false;
            }
        } else {
            if c >= 97 {
                if c <= 122 {
                    letters += 1;
                } else {
                    return false;
                }
            } else {
                return false;
            }
        }

        j += 1;
    }
    if letters < 2 {
        return false;
    }
    return true;
}

fn scanLink(line: &str, i: i64, isFinal: bool, seenCode: bool) -> Option<LinkScan> {
    let mut close = findStrFrom(line, "]", i);
    if close == -1 {
        return None;
    }
    let text = line.chars().take((close) as usize).skip((i + 1) as usize).collect::<String>();
    let mut after: i64 = close + 1;
    if !(startsWithAt(line, "(", after)) {
        return None;
    }
    let mut end = findStrFrom(line, ")", after);
    if end == -1 {
        



        let frag = line.chars().skip((after + 1) as usize).collect::<String>();
        if isHttpUrl(frag.as_str()) {
            let mut urlHref = stripTrailingUrlPunct(frag.as_str());
            let mut tailText = frag.chars().skip(((urlHref.chars().count() as i64)) as usize).collect::<String>();
            let mut urlTitle: Option<String> = Some("".to_string());
            if seenCode {
                urlTitle = None;
            }            return Some(LinkScan { next: (line.chars().count() as i64), text: text.to_string(), href: urlHref.to_string(), loading: true, title: urlTitle, tail: tailText.to_string() });
        }        if isBareDomain(frag.as_str()) {
            return Some(LinkScan { next: (line.chars().count() as i64), text: text.to_string(), href: format!("{}{}", "http://", frag).to_string(), loading: true, title: None, tail: "".to_string() });
        }        return Some(LinkScan { next: (line.chars().count() as i64), text: text.to_string(), href: "".to_string(), loading: true, title: None, tail: "".to_string() });
    }
    let mut inner = line.chars().take((end) as usize).skip((after + 1) as usize).collect::<String>();
    let mut href = inner.clone();
    let mut title: Option<String> = None;
    let sp = findStr(inner.as_str(), " \"");
    if sp != -1 {
        href = inner.chars().take((sp) as usize).skip((0) as usize).collect::<String>();
        let titlePart = inner.chars().skip((sp + 2) as usize).collect::<String>();
        if endsWithTokSeq(titlePart.as_str(), "\"") {
            title = Some(titlePart.chars().take(((titlePart.chars().count() as i64) - 1) as usize).skip((0) as usize).collect::<String>());
        }    } else {
        




        let mut lineTail: bool = end + 1 >= (line.chars().count() as i64);
        if !(lineTail) {
            if !(seenCode) {
                title = Some("".to_string());
            }        }    }

    return Some(LinkScan { next: end + 1, text: text.to_string(), href: href.to_string(), loading: false, title: title, tail: "".to_string() });
}

fn extractAnchorBlock(text: &str) -> String {
    let t = text.trim().to_string();
    let parts = t.split(" ").map(|s| s.to_string()).collect::<Vec<String>>();
    let n: i64 = (parts.len() as i64);
    if n < 2 {
        return "".to_string();
    }
    let last = parts[(n - 1) as usize].clone();
    if (last.chars().count() as i64) < 2 {
        return "".to_string();
    }
    if last.chars().take((1) as usize).skip((0) as usize).collect::<String>() != "^" {
        return "".to_string();
    }
    let body = last.chars().skip((1) as usize).collect::<String>();
    let alphabet: String = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789_-".to_string();
    for i in 0..(body.chars().count() as i64) {
        let ch = body.chars().take((i + 1) as usize).skip((i) as usize).collect::<String>();
        let mut ok: bool = false;
        for j in 0..(alphabet.chars().count() as i64) {
            if alphabet.chars().take((j + 1) as usize).skip((j) as usize).collect::<String>() == ch {
                ok = true;
            }
        }
        if !(ok) {
            return "".to_string();
        }
    }
    return body;
}

fn convertInlines(wnodes: Vec<WNode>, mut marks: Vec<Mark>) -> Vec<InlineSpan> {
    let mut out: Vec<InlineSpan> = vec![];
    for w in &wnodes {
        let t: String = w.r#type.clone();
        if t == "text" {
            out.push(spanWith(w.content.clone().unwrap_or("".to_string()).as_str(), marks.clone(), vec![]));
        }
        if t == "strong" {
            for s in convertInlines(w.children.clone().unwrap_or(noNodes()), addMark(marks.clone(), Mark::Strong.clone())) {
                out.push(s.clone());
            }
        }
        if t == "emphasis" {
            for s in convertInlines(w.children.clone().unwrap_or(noNodes()), addMark(marks.clone(), Mark::Em.clone())) {
                out.push(s.clone());
            }
        }
        if t == "underline" {
            for s in convertInlines(w.children.clone().unwrap_or(noNodes()), addMark(marks.clone(), Mark::Underline.clone())) {
                out.push(s.clone());
            }
        }
        if t == "strikethrough" {
            for s in convertInlines(w.children.clone().unwrap_or(noNodes()), addMark(marks.clone(), Mark::Del.clone())) {
                out.push(s.clone());
            }
        }
        if t == "inline_code" {
            out.push(spanWith(w.code.clone().unwrap_or("".to_string()).as_str(), addMark(marks.clone(), Mark::Code.clone()), vec![]));
        }
        if t == "hardbreak" {
            out.push(spanWith("\n", marks.clone(), vec![]));
        }
        if t == "link" {
            let mut lattrs: Vec<Attr> = vec![];
            lattrs = attrSet(lattrs.clone(), "href", Value::Str(w.href.clone().unwrap_or("".to_string())));
            let title = w.title.clone();
            if title != None {
                lattrs = attrSet(lattrs.clone(), "title", Value::Str(title.unwrap_or("".to_string())));
            }            for s in convertInlines(w.children.clone().unwrap_or(noNodes()), addMark(marks.clone(), Mark::Link.clone())) {
                out.push(spanWith(s.text.as_str(), s.marks.clone(), lattrs.clone()));
            }
        }
        if t == "image" {
            let mut iattrs: Vec<Attr> = vec![];
            iattrs = attrSet(iattrs.clone(), "src", Value::Str(w.src.clone().unwrap_or("".to_string())));
            iattrs = attrSet(iattrs.clone(), "alt", Value::Str(w.alt.clone().unwrap_or("".to_string())));
            let ititle = w.title.clone();
            if ititle != None {
                iattrs = attrSet(iattrs.clone(), "title", Value::Str(ititle.unwrap_or("".to_string())));
            }            out.push(spanWith(w.alt.clone().unwrap_or("".to_string()).as_str(), addMark(marks.clone(), Mark::Image.clone()), iattrs.clone()));
        }
    }
    return out;
}

fn convertTableCell(wnode: WNode, id: &str) -> BlockNode {
    let mut attrs: Vec<Attr> = vec![];
    attrs = attrSet(attrs.clone(), "header", Value::Bool(wnode.isHeader.unwrap_or(false)));
    attrs = attrSet(attrs.clone(), "align", Value::Str(wnode.align.unwrap_or("left".to_string())));
    return blockFull(id, BlockType::TableCell.clone(), attrs.clone(), vec![], convertInlines(wnode.children.unwrap_or(noNodes()), vec![]), rng(0, 0));
}

fn convertTableRow(wnode: WNode, id: &str) -> BlockNode {
    let cells: Vec<WNode> = wnode.cells.unwrap_or(noNodes());
    let mut kids: Vec<BlockNode> = vec![];
    for ci in 0..(cells.len() as i64) {
        kids.push(convertTableCell(cells[(ci) as usize].clone(), format!("{}{}", format!("{}{}", id, "-c"), format!("{:?}", ci)).as_str()));
    }
    return blockFull(id, BlockType::TableRow.clone(), vec![], kids.clone(), vec![], rng(0, 0));
}

fn convertBlock(wnode: WNode, id: &str) -> BlockNode {
    let t: String = wnode.r#type.clone();
    if t == "heading" {
        let mut attrs: Vec<Attr> = vec![];
        attrs = attrSet(attrs.clone(), "level", Value::Int(wnode.level.unwrap_or(0)));
        return blockFull(id, BlockType::Heading.clone(), attrs.clone(), vec![], convertInlines(wnode.children.unwrap_or(noNodes()), vec![]), rng(0, 0));
    }
    if t == "code_block" {
        let mut attrs2: Vec<Attr> = vec![];
        attrs2 = attrSet(attrs2.clone(), "language", Value::Str(wnode.language.unwrap_or("".to_string())));
        attrs2 = attrSet(attrs2.clone(), "loading", Value::Bool(wnode.loading.unwrap_or(false)));
        return blockFull(id, BlockType::Fence.clone(), attrs2.clone(), vec![], vec![span(wnode.code.unwrap_or("".to_string()).as_str())], rng(0, 0));
    }
    if t == "blockquote" {
        return blockFull(id, BlockType::Blockquote.clone(), vec![], convertChildren(wnode.children.unwrap_or(noNodes()), id), vec![], rng(0, 0));
    }
    if t == "list" {
        let mut attrs3: Vec<Attr> = vec![];
        attrs3 = attrSet(attrs3.clone(), "ordered", Value::Bool(wnode.ordered.unwrap_or(false)));
        let start: Option<i64> = wnode.start.clone();
        if start != None {
            attrs3 = attrSet(attrs3.clone(), "start", Value::Int(start.unwrap_or(0)));
        }        return blockFull(id, BlockType::ListBlock.clone(), attrs3.clone(), convertChildren(wnode.items.unwrap_or(noNodes()), id), vec![], rng(0, 0));
    }
    if t == "list_item" {
        return blockFull(id, BlockType::ListItem.clone(), vec![], convertChildren(wnode.children.unwrap_or(noNodes()), id), vec![], rng(0, 0));
    }
    if t == "table" {
        let mut kids: Vec<BlockNode> = vec![];
        let hdr: Vec<WNode> = wnode.header.unwrap_or(noNodes());
        if (hdr.len() as i64) > 0 {
            kids.push(convertTableRow(hdr[0].clone(), format!("{}{}", id, "-h").as_str()));
        }        let rows: Vec<WNode> = wnode.rows.unwrap_or(noNodes());
        for ri in 0..(rows.len() as i64) {
            kids.push(convertTableRow(rows[(ri) as usize].clone(), format!("{}{}", format!("{}{}", id, "-r"), format!("{:?}", ri)).as_str()));
        }
        let mut attrs4: Vec<Attr> = vec![];
        attrs4 = attrSet(attrs4.clone(), "loading", Value::Bool(wnode.loading.unwrap_or(false)));
        return blockFull(id, BlockType::Table.clone(), attrs4.clone(), kids.clone(), vec![], rng(0, 0));
    }
    if t == "thematic_break" {
        return blockFull(id, BlockType::ThematicBreak.clone(), vec![], vec![], vec![], rng(0, 0));
    }
    if t == "callout" {
        let mut attrsC: Vec<Attr> = vec![];
        attrsC = attrSet(attrsC.clone(), "type", Value::Str(wnode.language.unwrap_or("".to_string())));
        attrsC = attrSet(attrsC.clone(), "title", Value::Str(wnode.title.unwrap_or("".to_string())));
        return blockFull(id, BlockType::Callout.clone(), attrsC.clone(), convertChildren(wnode.children.unwrap_or(noNodes()), id), vec![], rng(0, 0));
    }
    if t == "details" {
        let mut attrsD: Vec<Attr> = vec![];
        attrsD = attrSet(attrsD.clone(), "summary", Value::Str(wnode.text.unwrap_or("".to_string())));
        

        if wnode.loading.unwrap_or(false) {
            attrsD = attrSet(attrsD.clone(), "open", Value::Bool(true));
        }        return blockFull(id, BlockType::Details.clone(), attrsD.clone(), convertChildren(wnode.children.unwrap_or(noNodes()), id), vec![], rng(0, 0));
    }
    if t == "query" {
        let mut attrsQ: Vec<Attr> = vec![];
        attrsQ = attrSet(attrsQ.clone(), "query", Value::Str(wnode.content.unwrap_or("".to_string())));
        return blockFull(id, BlockType::QueryBlock.clone(), attrsQ.clone(), vec![], vec![], rng(0, 0));
    }
    if t == "embed" {
        let mut attrsE: Vec<Attr> = vec![];
        attrsE = attrSet(attrsE.clone(), "src", Value::Str(wnode.src.unwrap_or("".to_string())));
        return blockFull(id, BlockType::BlockEmbed.clone(), attrsE.clone(), vec![], vec![], rng(0, 0));
    }

    return blockFull(id, BlockType::Paragraph.clone(), vec![], vec![], convertInlines(wnode.children.unwrap_or(noNodes()), vec![]), rng(0, 0));
}

fn convertChildren(mut wchildren: Vec<WNode>, parentId: &str) -> Vec<BlockNode> {
    let mut out: Vec<BlockNode> = vec![];
    for i in 0..(wchildren.len() as i64) {
        out.push(convertBlock(wchildren[(i) as usize].clone(), format!("{}{}", format!("{}{}", parentId, "-"), format!("{:?}", i)).as_str()));
    }
    return out;
}

fn intListToValue(mut arr: Vec<Option<i64>>) -> Value {
    let mut out: Vec<Value> = vec![];
    let mut i: i64 = 0;
    while i < (arr.len() as i64) {
        let v = arr[(i) as usize].clone();
        if v == None {
            out.push(Value::Null);
        } else {
            out.push(Value::Int(v.unwrap_or(0)));
        }

        i += 1;
    }
    return Value::ListV(out);
}

fn attachIAL(blocks: Vec<BlockNode>, mut ialAttrs: Vec<TableAttr>) -> Vec<BlockNode> {
    let mut out: Vec<BlockNode> = vec![];
    let mut ti: i64 = 0;
    for b in &blocks {
        if b.kind == BlockType::Table {
            if ti < (ialAttrs.len() as i64) {
                let ta = ialAttrs[(ti) as usize].clone();
                let cols = intListToValue(ta.cols.clone());
                let rows = intListToValue(ta.rows.clone());
                let mut ialPair: Vec<Attr> = vec![];
                ialPair.push(attrOf("cols", cols.clone()));
                ialPair.push(attrOf("rows", rows.clone()));
                out.push(blockFull(b.id.as_str(), b.kind.clone(), attrSet(b.attrs.clone(), "ial", Value::AttrsV(ialPair)), b.children.clone(), b.inlines.clone(), b.source.clone()));
                ti += 1;
            } else {
                out.push(b.clone().clone());
            }
        } else {
            out.push(b.clone().clone());
        }

    }
    return out;
}

fn withAnchorId(node: BlockNode, fallbackId: &str) -> BlockNode {
    let anchor = extractAnchorBlock(spansText(node.inlines.clone()).as_str());
    if anchor == "" {
        return node;
    }
    return blockFull(anchor.as_str(), node.kind.clone(), node.attrs.clone(), node.children.clone(), node.inlines.clone(), node.source.clone());
}

fn endsWithTokSeq(hay: &str, needle: &str) -> bool {
    let hl: i64 = (hay.chars().count() as i64);
    let nl: i64 = (needle.chars().count() as i64);
    if nl > hl {
        return false;
    }
    return hay.chars().take((hl) as usize).skip((hl - nl) as usize).collect::<String>() == needle;
}

fn stripAnchorSpans(mut spans: Vec<InlineSpan>, cut: i64) -> Vec<InlineSpan> {
    let mut out: Vec<InlineSpan> = vec![];
    let mut pos: i64 = 0;
    for i in 0..(spans.len() as i64) {
        let t = spans[(i) as usize].clone().text;
        let start: i64 = pos;
        let end: i64 = pos + (t.chars().count() as i64);
        pos = end;
        if start >= cut {
            

        } else {
            if end <= cut {
                out.push(spans[(i) as usize].clone());
            } else {
                let keepLen: i64 = cut - start;
                if keepLen > 0 {
                    out.push(spanWith(t.chars().take((keepLen) as usize).skip((0) as usize).collect::<String>().as_str(), spans[(i) as usize].clone().marks.clone(), spans[(i) as usize].clone().attrs.clone()));
                }            }
        }

    }
    return out;
}

fn isAnchorableLeafKind(kind: BlockType) -> bool {
    if kind == BlockType::Paragraph {
        return true;
    }
    if kind == BlockType::Heading {
        return true;
    }
    if kind == BlockType::ListItem {
        return true;
    }
    return false;
}

pub fn applyAnchorsDeep(node: BlockNode) -> BlockNode {
    let mut kids: Vec<BlockNode> = vec![];
    for i in 0..(node.children.len() as i64) {
        kids.push(applyAnchorsDeep(node.children[(i) as usize].clone()));
    }
    let mut out: BlockNode = blockFull(node.id.as_str(), node.kind.clone(), node.attrs.clone(), kids.clone(), node.inlines.clone(), node.source.clone());
    if isAnchorableLeafKind(node.kind.clone()) {
        if (node.inlines.len() as i64) > 0 {
            let text = spansText(node.inlines.clone());
            let anchor = extractAnchorBlock(text.as_str());
            if anchor != "" {
                let total: i64 = (text.chars().count() as i64);
                let tokLen: i64 = (anchor.chars().count() as i64) + 1;
                let spaceIdx: i64 = total - tokLen - 1;
                if spaceIdx >= 0 {
                    let ws = text.chars().take((spaceIdx + 1) as usize).skip((spaceIdx) as usize).collect::<String>();
                    if ws == " " || ws == "\t" {
                        let stripped = stripAnchorSpans(node.inlines.clone(), spaceIdx);
                        let base = blockFull(node.id.as_str(), node.kind.clone(), node.attrs.clone(), kids.clone(), stripped.clone(), node.source.clone());
                        out = withIdAndAnchor(base.clone(), anchor.as_str());
                    }                }            }        }    }
    return out;
}

fn stripLineAnchor(l: &str) -> String {
    let n: i64 = (l.chars().count() as i64);
    if n < 3 {
        return l.to_string();
    }
    let cand = extractAnchorBlock(l);
    if cand == "" {
        return l.to_string();
    }
    let tok: String = format!("{}{}", "^", cand);
    let tokLen: i64 = (tok.chars().count() as i64);
    if !(endsWithTokSeq(l, tok.as_str())) {
        return l.to_string();
    }
    let before: i64 = n - tokLen;
    if before <= 0 {
        return l.to_string();
    }
    let ws = l.chars().take((before) as usize).skip((before - 1) as usize).collect::<String>();
    if ws != " " && ws != "\t" {
        return l.to_string();
    }
    return l.chars().take((before - 1) as usize).skip((0) as usize).collect::<String>().to_string();
}

pub fn stripAnchorTokens(md: &str) -> String {
    let mut out: String = "".to_string();
    let mut inFence: bool = false;
    let lines = md.split("\n").map(|s| s.to_string()).collect::<Vec<String>>();
    let count: i64 = (lines.len() as i64);
    for i in 0..count {
        let mut l: String = lines[(i) as usize].clone();
        if l.chars().take((3) as usize).skip((0) as usize).collect::<String>() == "```" {
            inFence = !(inFence);
        } else {
            if !(inFence) {
                l = stripLineAnchor(l.as_str());
            }        }

        out = format!("{}{}", out, l);
        if i < count - 1 {
            out = format!("{}{}", out, "\n");
        }
    }
    return out;
}

pub fn parse_blocks(src: &str, isFinal: bool) -> BlockNode {
    let pre = preprocessMarkdown(src);
    let preMd = pre.md.clone();
    let weak = parseDocument(preMd.as_str(), isFinal);
    let mut kids: Vec<BlockNode> = vec![];
    for i in 0..(weak.len() as i64) {
        let fallback: String = format!("{}{}", "block-", format!("{:?}", i));
        kids.push(applyAnchorsDeep(convertBlock(weak[(i) as usize].clone(), fallback.as_str())));
    }
    let withIal = attachIAL(kids.clone(), pre.tableAttrs.clone());
    return blockFull("doc", BlockType::Paragraph.clone(), vec![], withIal.clone(), vec![], rng(0, 0));
}
