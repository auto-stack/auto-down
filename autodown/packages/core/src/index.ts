// @autodown/core — re-export shim (plan 017 Phase 1). The parser/kernel
// sources moved to @autodown/engine (packages/engine/src/parser); this
// package forwards its old public surface unchanged.
export * from '@autodown/engine/parser'
