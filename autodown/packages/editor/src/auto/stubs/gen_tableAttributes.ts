// gen_tableAttributes.ts — gen-project stub for
// src/extensions/tableAttributes.ts (the real module pulls in @tiptap/pm,
// which the self-contained gen project lacks). Mirrored into
// gen/front/vue/src/extensions/tableAttributes.ts by the regen script.
// Never ships: the copied src/core/AutoDownEditor.vue resolves the real
// module in the editor tree. Only `appendTableIAL` is consumed (via the
// auto_down_editor_ext re-export).

export function appendTableIAL(md: string, _editor: any): string {
  return md
}
