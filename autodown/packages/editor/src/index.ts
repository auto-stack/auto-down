// @autodown/editor — re-export shim (plan 017 Phase 1). The Tiptap platform
// layer moved to @autodown/engine (packages/engine/src/editor); this package
// forwards its old public surface unchanged. Kernel replacement is plan 018.
export * from '@autodown/engine/editor'
