<script setup lang="ts">
// gen_menus/SlashMenu.vue — Gen-project stub for the real
// src/menus/SlashMenu.vue (Auto-generated from slash_menu.at in the editor
// package). Mirrored into gen/front/vue/src/menus/SlashMenu.vue by the
// regen script so the gen-only auto_down_editor_ext.ts copy's re-export
// resolves. Never ships: the copied src/core/AutoDownEditorInner.vue
// resolves the real SFC in the editor tree. Props are the union the
// generated inner SFC binds.
defineProps<{
  editor?: any
  items?: any[]
  noResultsText?: string
  linkPrompt?: string
  tooltips?: any
  titles?: any
}>()
</script>

<template>
  <div />
</template>
