// gen_useAutoDownEditor.ts — Gen-project stub for
// src/composables/useAutoDownEditor.ts (the real tiptap editor factory).
// Mirrored into gen/front/vue/src/composables/useAutoDownEditor.ts by the
// regen script. Never ships: the copied src/core/AutoDownEditorInner.vue
// resolves the real module in the editor tree. The gen project has no
// @tiptap/@autodown dependency; the stub only needs to type-check the
// bridge's usage (call with an options object, watch the returned ref).
import { ref } from 'vue'

export function useAutoDownEditor(_options: any) {
  return ref<any>(null)
}
