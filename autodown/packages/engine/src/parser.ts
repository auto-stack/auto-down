// @autodown/engine — ./parser exit (plan 017). Kernel layer: block model,
// incremental markdown parser, serializer, IAL. Vue-free by construction
// (asserted in CI per plan 017 Phase 1).
export * from './parser/index'
