// @autodown/engine — ./render exit (plan 017). Render layer: streaming
// document model, scheduler, MarkdownRender / StreamingRenderer / tables,
// optional capabilities (katex / mermaid / highlight).
export * from './render/index'
