<!-- gen-components/FlashcardModal.vue — gen-project stub for the front
     tree's front/src/components/FlashcardModal.vue (not yet Auto-translated).
     Same NEVER-SHIPS dual-resolution role as the sibling stubs; carries the
     `open` prop + update:open emit because AppShell binds v-model:open. -->
<script setup lang="ts">
defineProps<{ open?: boolean }>()
defineEmits<{ 'update:open': [value: boolean] }>()
</script>

<template>
  <div />
</template>
