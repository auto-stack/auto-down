<!-- gen-components/PropertiesPanel.vue — gen-project stub for the front tree's
     front/src/components/PropertiesPanel.vue (not yet Auto-translated).
     Mirrored into gen/front/vue/src/components/PropertiesPanel.vue by the widget
     Regenerate flow so the batch-3 extension modules (which re-export the
     real component via the dual-resolution relative path) type-check inside
     the self-contained gen project. NEVER SHIPS. When PropertiesPanel is itself
     translated, the compiler emits the real SFC over this stub. -->
<script setup lang="ts">
</script>

<template>
  <div />
</template>
