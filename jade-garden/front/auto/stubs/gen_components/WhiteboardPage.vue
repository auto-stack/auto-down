<!-- gen-components/WhiteboardPage.vue — gen-project stub for the front tree's
     front/src/components/WhiteboardPage.vue (not yet Auto-translated —
     Phase 5.3d).
     Mirrored into gen/front/vue/src/components/WhiteboardPage.vue by the
     widget Regenerate flow so main_area_ext (which re-exports the real
     component via the dual-resolution relative path) type-checks inside the
     self-contained gen project. NEVER SHIPS. When WhiteboardPage is itself
     translated, the compiler emits the real SFC over this stub. -->
<script setup lang="ts">
</script>

<template>
  <div />
</template>
