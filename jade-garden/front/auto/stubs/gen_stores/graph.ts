// gen_stores/graph.ts — gen-project stub for the front tree's
// front/src/stores/graph.ts FACADE.
//
// graph_controls_ext re-exports `useGraphStore` from
// '../../../../src/stores/graph' (dual-resolution shim): in the front tree
// that resolves to the real facade; in the gen project it resolves to
// gen/front/vue/src/stores/graph.ts — this file, mirrored here by the
// Regenerate flow. NEVER SHIPS: the copied SFC in front/src/components/
// resolves the real facade.
//
// `any` return so every facade member the widget touches type-checks.

export function useGraphStore(): any {
  return {}
}
