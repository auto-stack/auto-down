// gen_stores/sidebar.ts — gen-project stub for the front tree's
// front/src/stores/sidebar.ts FACADE (same dual-resolution pattern as
// gen_stores/tabs.ts). NEVER SHIPS.

export function useSidebarStore(): any {
  return {}
}

// Batch 3: type re-export needed by ribbon_ext (gen-side type-check only).
export type LeftPanel = any
