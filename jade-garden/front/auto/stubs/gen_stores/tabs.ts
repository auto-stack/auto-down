// gen_stores/tabs.ts — gen-project stub for the front tree's
// front/src/stores/tabs.ts FACADE.
//
// Panel widget extensions re-export `useTabsStore` from
// '../../../../src/stores/tabs' (dual-resolution shim): in the front tree
// that resolves to the real facade; in the gen project it resolves to
// gen/front/vue/src/stores/tabs.ts — this file, mirrored here by the
// Regenerate flow. NEVER SHIPS: the copied SFC in front/src/components/
// resolves the real facade.
//
// `any` return so every facade member the widgets touch type-checks.

export function useTabsStore(): any {
  return {}
}
