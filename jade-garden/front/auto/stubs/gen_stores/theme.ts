// gen_stores/theme.ts — gen-project stub for the front tree's
// front/src/stores/theme.ts FACADE (same dual-resolution pattern as
// gen_stores/tabs.ts). NEVER SHIPS.

export function useThemeStore(): any {
  return {}
}

// Batch 3: type re-export needed by theme_popover_ext (gen-side type-check
// only).
export type ThemeAccent = any
