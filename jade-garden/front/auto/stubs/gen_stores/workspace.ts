// gen_stores/workspace.ts — gen-project stub for the front tree's
// front/src/stores/workspace.ts FACADE (same dual-resolution pattern as
// gen_stores/tabs.ts). NEVER SHIPS.

export function useWorkspaceStore(): any {
  return {}
}
